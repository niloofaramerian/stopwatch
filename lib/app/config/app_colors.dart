import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF243042);
  static const onPrimary = Color(0xFFFFFFFF);

  static const secondary = Color(0xFFFE9677);
  static const onSecondary = Color(0xFF283447);

  static const surface = Color(0xFF283447);

  static const secondaryText = Color(0xFFC3C3C3);

  static Color shadow = Colors.black54.withOpacity(0.2);
}
